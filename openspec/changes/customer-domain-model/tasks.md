# Tasks: Customer Domain Model

- [ ] Update `Backend/app/models.py` to add `role` field to `User` entity.
- [ ] Implement `Customer` entity in `Backend/app/models.py` with required fields and `salesperson_id` relationship.
- [ ] Run database migration script (or equivalent setup logic) to apply new schema changes.
- [ ] Update `Backend/app/schemas.py` to include new `Customer` Pydantic models for API interactions.
- [ ] Create basic unit tests to verify the `Customer` and `User` model relationships and constraints.
