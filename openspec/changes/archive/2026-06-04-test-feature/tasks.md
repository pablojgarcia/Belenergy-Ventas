# Tasks: Test Feature

- [ ] Create a dummy file in `Ventas/lib/` named `test_file.dart`.
- [ ] Add a simple comment inside `test_file.dart`.
- [ ] Verify the file creation.
- [ ] Archive the `test-feature` change using `openspec-archive-change`.
